import styled from "styled-components";

export const EndMessage = styled.p`
  margin: 1rem auto;
  margin-bottom: 2rem;
  width: 100%;
  text-align: center;
  font-size: ${props => props.theme.black};
  font-size: 2.4rem;
`;
